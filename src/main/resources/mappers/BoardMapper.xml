<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	<select id="selectTest" parameterType="int" resultType="com.daesungra.domain.BoardVo">
		SELECT serial, title, content, category, userId, gSerial, pSerial, depth, indent, to_char(bDate, 'YY/MM/DD HH24:MI:SS') bDate
			FROM DBOARD
			WHERE category = #{category}
	</select>
</mapper>